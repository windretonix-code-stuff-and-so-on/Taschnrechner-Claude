// Transparent Canvas 2D reactive particle system (idle mist, accumulation,
// dissolve/rematerialize, condensation, turbulence). Filled in on
// feature/animation-particles.

export function createSmokeRenderer(_canvas) {
  throw new Error('createSmokeRenderer() not implemented yet');
}

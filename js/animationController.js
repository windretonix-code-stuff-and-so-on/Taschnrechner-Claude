// Result (~1.4s) and error/wizard (~3.5s) animation sequences, with
// deterministic C-cancellation. Filled in on feature/animation-particles.

export function createAnimationController(_deps) {
  throw new Error('createAnimationController() not implemented yet');
}

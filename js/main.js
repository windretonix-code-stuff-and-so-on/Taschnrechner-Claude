// Orchestration entry point. Wires up state, input, display, animation,
// smoke, audio and layout modules. Filled in across the later PLAN.md
// branches (core-math-engine, layout-structure, visual-design,
// animation-particles, audio-candle).

document.addEventListener('DOMContentLoaded', () => {
  const app = document.getElementById('app');
  app.innerHTML = '<p style="color:#8ab4ff;font-family:sans-serif">Magic Orb Calculator — Gerüst (feature/project-setup)</p>';
});
